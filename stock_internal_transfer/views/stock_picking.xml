<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_stock_form" model="ir.ui.view">
        <field name="name">view.stock.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='state']" position="inside">
                <field name="state" widget="statusbar" statusbar_visible="draft,ready,approve_pending,done"/>
            </xpath>
            <xpath expr="//header" position="inside">
                <button name="action_second_approve" type="object"
                        string="Second Approval" class="btn btn-primary" invisible="state =='done'"
                        groups="stock.group_stock_manager"/>
            </xpath>
            <xpath expr="//form/header/button[@name='button_validate']" position="attributes">
                <attribute name="invisible">state != 'draft' or state!='waiting' or state=='approve_pending'</attribute>
            </xpath>
            <xpath expr="//field[@name='move_type']" position="after">
                <field name="approver_id" domain="[('user_id','!=',user_id)]" readonly="not user_id"/>
            </xpath>

        </field>
    </record>
</odoo>